---
  - name: Shell Test Example
    hosts: all
    tasks:
    
    - name: List Ansible Working folder
      shell: |
        echo "~{{ ansible_user }}"
        cd "~{{ ansible_user }}"
        pwd
        
      register: fileout
      tags: fileout
      
    - debug: msg="{{ fileout.stdout }}"
