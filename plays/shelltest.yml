---
  - name: Shell Test Example
    hosts: all
    tasks:
    
    - name: List Ansible Working folder
      shell:
        ls -laR ~/.ansible
      register: pwdcmd
      tags: pwdcmd
      
    - debug: msg="{{pwdcmd.stdout}}"
